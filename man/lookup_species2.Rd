% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup_species2.R
\name{lookup_species2}
\alias{lookup_species2}
\title{BC Species Lookup Table}
\usage{
lookup_species2()
}
\value{
A data.table with species information containing the following columns:
\describe{
  \item{SPECIES}{Species code used in BC forestry}
  \item{SP0}{Simplified species group code}
  \item{DESCRIPTION}{Full species name}
  \item{SP_SINDEX}{Species code used for site index calculations}
  \item{SP_TYPE}{Species type: 'C' for conifer, 'D' for deciduous}
  \item{SP_COST}{Species cost category}
  \item{LONG_SPECIES}{Scientific name or extended identifier}
}
}
\description{
Creates a comprehensive lookup table for tree species codes used in British Columbia
forestry applications. This function provides standardized species information including
species codes, descriptions, site index species, and classification into conifer/deciduous types.
}
\details{
This lookup table is essential for processing VRI data and VDYP7 outputs,
particularly for calculating hardwood vs softwood volume components. The SP_TYPE
column is used to classify species as coniferous ('C') or deciduous ('D'), which
is crucial for volume calculations.

The data is based on the FAIBCompiler package species lookup table and includes
all major tree species found in British Columbia forests.
}
\examples{
# Get the species lookup table
species_table <- lookup_species2()

# View structure
head(species_table)

# Check available species codes
unique(species_table$SPECIES)

# Find coniferous species
conifers <- species_table[SP_TYPE == "C"]

# Find deciduous species
deciduous <- species_table[SP_TYPE == "D"]

}
